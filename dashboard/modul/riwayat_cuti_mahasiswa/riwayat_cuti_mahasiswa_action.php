<?php
session_start();
include "../../inc/config.php";
session_check();
switch ($_GET["act"]) {
  case "in":
    
  
  
  
  $data = array(
  );
  
  
  
   
    $in = $db->insert("cuti_mahasiswa",$data);
    
    
    if ($in=true) {
      echo "good";
    } else {
      return false;
    }
    break;
  case "delete":
    
    
    
    $db->delete("cuti_mahasiswa","id_cuti",$_GET["id"]);
    break;
   case "del_massal":
    $data_ids = $_REQUEST["data_ids"];
    $data_id_array = explode(",", $data_ids);
    if(!empty($data_id_array)) {
        foreach($data_id_array as $id) {
          $db->delete("cuti_mahasiswa","id_cuti",$id);
         }
    }
    break;
  case "up":
    
   $data = array(
   );
   
   
   

    
    
    $up = $db->update("cuti_mahasiswa",$data,"id_cuti",$_POST["id"]);
    
    if ($up=true) {
      echo "good";
    } else {
      return false;
    }
    break;
  default:
    # code...
    break;
}

?>